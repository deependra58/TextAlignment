package cqu.textalignment;

import java.util.LinkedList;

public class LeftAligned extends AlignmentStrategy {

    /**
     * The lines will already be left aligned so there is no work to be done
     */
    public LeftAligned() {
    }

    @Override
    void align(LinkedList<String> line) {

    }
}
